<sw-page class="shopbite-business_hour-detail">
    <template #smart-bar-actions>
        <sw-button :routerLink="{ name: 'shopbite.business.hour.list' }">
            {{ $tc('shopbite-business_hour.detail.buttonCancel') }}
        </sw-button>

        <sw-button-process
            variant="primary"
            :isLoading="isLoading"
            :processSuccess="processSuccess"
            @process-finish="processSuccess = false"
            @click="onSave"
        >
            {{ $tc('shopbite-business_hour.detail.buttonSave') }}
        </sw-button-process>
    </template>

    <template #content>
        <sw-card v-if="businessHour" :isLoading="isLoading">
            <sw-entity-multi-select
                v-model:entityCollection="salesChannels"
                entity="sales_channel"
                :label="$tc('shopbite-business_hour.detail.labelSalesChannel')"
                required
            />

            <sw-multi-select
                v-model:value="selectedDays"
                :options="days"
                :label="$tc('shopbite-business_hour.detail.labelDayOfWeek')"
                required
            />

            <sw-datepicker
                v-model:value="businessHour.openingTime"
                dateType="time"
                :label="$tc('shopbite-business_hour.detail.labelOpeningTime')"
                required
            />

            <sw-datepicker
                v-model:value="businessHour.closingTime"
                dateType="time"
                :label="$tc('shopbite-business_hour.detail.labelClosingTime')"
                required
            />
        </sw-card>
    </template>
</sw-page>
